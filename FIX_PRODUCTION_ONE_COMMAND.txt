========================================
FIX PRODUCTION DATABASE - ONE COMMAND
========================================

Copy and paste this entire command into your terminal:

ssh root@76.13.48.245 "cd /var/www/iqrab3 2>/dev/null || cd /root/iqrab3 2>/dev/null || cd ~/iqrab3; git pull origin main; cd backend; node scripts/quick-fix-table.js; pm2 restart iqrab3-backend || pm2 restart bilal-backend || pm2 restart all; pm2 status"

========================================

This will:
1. SSH into your VPS
2. Navigate to project
3. Pull latest changes
4. Fix the database
5. Restart backend
6. Show status

After running, test: https://iqrab3.schoolific.com/hr/leave

========================================

If the one-liner doesn't work, follow these manual steps:

1. SSH into VPS:
   ssh root@76.13.48.245

2. Navigate to project:
   cd /var/www/iqrab3/backend

3. Pull changes:
   git pull origin main

4. Fix database:
   node scripts/quick-fix-table.js

5. Restart backend:
   pm2 restart iqrab3-backend

6. Test website:
   https://iqrab3.schoolific.com/hr/leave

========================================
